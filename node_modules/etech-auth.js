const VALID_USER_NAME = 'minecraft';
const VALID_PASSWORD = 'Balance2016';

function authorize(user_name, user_password) {
    if (user_name !== VALID_USER_NAME) {
        return false;
    }
    if (user_password !== VALID_PASSWORD) {
        return false;
    }
    return true;
}
exports.authorize = authorize;